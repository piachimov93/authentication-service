CREATE TABLE IF NOT EXISTS t_users (
                                    username VARCHAR(50) NOT NULL,
                                    password TEXT NULL,
                                    PRIMARY KEY (username)
);