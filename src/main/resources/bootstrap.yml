spring:
  application:
    name: authentication-server
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8071

logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.example: DEBUG